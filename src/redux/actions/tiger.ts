import { footVo } from "../reducers/foot"
export const increTiger = (value: footVo) => ({ type: 'SET_SIDEBAR_FOLD', value })
export const delTigerById = (value: footVo) => ({ type: 'SET_START', value })
